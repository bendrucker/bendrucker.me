box: wercker/nodejs
step: lukevivier/gh-pages@0.2.1
build:
    steps:
        - npm-install
        - script:
            name: wintersmith build
            code: ./node_modules/.bin/wintersmith build -o ./build
depoy:
    steps:
        - gh-pages:
            token: $GH_TOKEN
            basedir: build
            domain: www.bendrucker.me