box: wercker/nodejs
build:
    steps:
        - npm-install
        - script:
            name: wintersmith build
            code: ./node_modules/.bin/wintersmith build -o ./build
depoy:
    steps:
        - lukevivier/gh-pages:
            token: $GH_TOKEN
            basedir: build
            domain: bendrucker.me