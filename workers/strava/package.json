{
  "name": "@workspace/strava-worker",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run types:generate && tsc --noEmit",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "types:generate": "wrangler types"
  },
  "dependencies": {
    "@workspace/logger": "*",
    "strava": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.27.0"
  }
}
